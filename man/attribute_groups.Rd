% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{attribute_groups}
\alias{attribute_groups}
\title{Attribute Grouping Vectors for Subgroup Analysis}
\usage{
attribute_groups(qdata, min_n = 0)
}
\arguments{
\item{qdata}{A QsortData object with participant attributes}

\item{min_n}{Minimum group size; factor levels with fewer participants
are set to NA and dropped (default 0, keep all)}
}
\value{
A named list of factors and numeric vectors, one per attribute
}
\description{
Build one grouping vector per participant attribute, aligned to
\code{qdata@participants}, by applying each stored specification with
\code{\link[=bin_attribute]{bin_attribute()}}. Numeric attributes without breaks are returned as
numeric vectors (not factors) so the caller can run a correlation
instead of a group test. Every vector carries the attribute type in
\code{attr(x, "type")}.
}
\examples{
\dontrun{
groups <- attribute_groups(qdata, min_n = 3)
}
}
