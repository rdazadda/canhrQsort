% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-generic.R
\name{read_qsort_csv}
\alias{read_qsort_csv}
\title{Read Q-Sort Data from CSV File}
\usage{
read_qsort_csv(
  file,
  format = c("wide", "long"),
  participant_col = "participant",
  statement_col = "statement",
  value_col = "value",
  statements = NULL,
  distribution = NULL,
  skip = 0,
  id_col = NULL,
  na_values = c("", "NA", "N/A", "."),
  ...
)
}
\arguments{
\item{file}{Path to the CSV file}

\item{format}{Character; data format - "wide" (default) or "long"}

\item{participant_col}{For long format, column name containing participant IDs}

\item{statement_col}{For long format, column name containing statement IDs}

\item{value_col}{For long format, column name containing sort values}

\item{statements}{Character vector of statement texts (optional)}

\item{distribution}{Numeric vector defining forced distribution (optional)}

\item{skip}{Number of rows to skip at the beginning of the file}

\item{id_col}{For wide format, column name/index containing participant IDs (NULL = use row names)}

\item{na_values}{Character vector of strings to interpret as NA}

\item{...}{Additional arguments passed to readr::read_csv}
}
\value{
A QsortData object
}
\description{
Import Q-sort data from a CSV file. The function handles multiple common
formats including wide format (participants as rows, statements as columns)
and long format (one row per participant-statement combination).
}
\examples{
\dontrun{
# Wide format (most common)
qdata <- read_qsort_csv("my_qsorts.csv")

# Wide format with first column as participant ID
qdata <- read_qsort_csv("my_qsorts.csv", id_col = 1)

# Long format
qdata <- read_qsort_csv(
  "my_qsorts_long.csv",
  format = "long",
  participant_col = "id",
  statement_col = "item",
  value_col = "rank"
)
}
}
