% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{match_participant_attributes}
\alias{match_participant_attributes}
\title{Match Attribute Rows to Q-Sorts}
\usage{
match_participant_attributes(participants, attrs, id_col = NULL)
}
\arguments{
\item{participants}{Character vector of participant ids, in sort order}

\item{attrs}{A data.frame of attribute rows}

\item{id_col}{Optional name of the column in \code{attrs} that holds the ids}
}
\value{
A list with elements:
\itemize{
\item \code{id_col}: name of the id column used
\item \code{matched}: integer count of participants matched to a row
\item \code{unmatched_participants}: participants with no attribute row
\item \code{unmatched_rows}: id values of rows that matched no participant
\item \code{duplicate_ids}: id values that appeared more than once (first kept)
\item \code{normalized}: TRUE when prefix normalization was needed
\item \code{aligned}: data.frame with \code{participant} first, then every attrs
column except the id column, NA for unmatched participants
\item \code{messages}: character vector of human sentences describing the match
}
}
\description{
Match the rows of an attribute table to a vector of participant ids
(typically \code{qdata@participants}, which are the sort column names of the
original data). The id column is chosen automatically unless \code{id_col}
names it: the column whose values overlap the participants the most (as
trimmed lowercase strings) wins, with ties broken in favor of columns
named like id, participant, or record. When no value matches exactly,
matching is retried after removing a leading qsort, q, p, or s prefix
(plus padding zeros) from both sides, so that ids like \code{qsort7} match \code{7}.
}
\examples{
\dontrun{
res <- match_participant_attributes(qdata@participants, attrs)
res$messages
}
}
