% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorities.R
\name{compute_priorities}
\alias{compute_priorities}
\title{Compute Ranked Priorities With Subgroup Tests}
\usage{
compute_priorities(qdata, groups = NULL, q = 0.05, n_perm = 9999, seed = 1)
}
\arguments{
\item{qdata}{A \code{QsortData} object.}

\item{groups}{NULL to use \code{attribute_groups(qdata)} when participant
attributes exist (an empty set otherwise), or a named list in the shape
\code{attribute_groups()} returns: per attribute either a factor aligned to
participants (with \code{attr(x, "type")} of "categorical" or "ordinal",
ordered factors for ordinal) or a numeric vector.}

\item{q}{False discovery rate threshold for flagging (default 0.05).}

\item{n_perm}{Number of label permutations for the PERMANOVA gate
(default 9999).}

\item{seed}{Fixed seed for the gate permutations and any Monte Carlo
test distributions (default 1). The caller's RNG state is restored.}
}
\value{
An object of class \code{"qsort_priorities"}: a list with elements
\verb{$table} (ranked statement table with one label column per tested
attribute), \verb{$tests} (long per-statement test results), \verb{$gates}
(per-attribute PERMANOVA gate), \verb{$concordance} (Kendall's W overall and
\verb{$per_level}), and \verb{$meta} (n, J, mu0, q, timestamp, warnings, families).
}
\description{
Builds the full priorities layer for a Q-sort dataset: a ranked table of
statement means with vs-neutral tests, per-attribute nonparametric
subgroup comparisons guarded by an attribute-level PERMANOVA gate,
Kendall's W concordance overall and per group level, and metadata.

Test engine, per family:
\itemize{
\item Vs neutral: \code{coin::wilcoxsign_test()} against the deck neutral mu0 with
\code{zero.method = "Pratt"}; exact distribution when at most 25 values differ
from mu0, asymptotic otherwise. Effect is the matched-pairs rank-biserial
correlation.
\item Two levels: \code{coin::wilcox_test()}; exact when the smaller group has fewer
than 10 members (falling back to \code{approximate(nresample = 100000)} if the
exact distribution is infeasible), asymptotic otherwise. Effect is the
probability of superiority computed from joint mid-ranks; the
Hodges-Lehmann shift and interval come from \code{stats::wilcox.test()}.
\item Three or more unordered levels: \code{coin::kruskal_test()}, using
\code{approximate(nresample = 100000)} when any included level has 5 to 9
members. Effect is epsilon squared (statistic / (n_used - 1)); direction
is only reported after a manual Dunn post hoc (BH across the pairwise
set) confirms a top contrast.
\item Ordered factors with three or more levels: \code{coin::lbl_test()}
(linear-by-linear association, raw sort values as response scores);
direction follows the sign of the standardized statistic.
\item Numeric attributes: \code{stats::cor.test(method = "spearman", exact = FALSE)};
effect is rho.
}

Guards: attribute levels with fewer than 5 members are excluded from
testing and noted; attributes left with fewer than 2 usable levels are
skipped; missing attribute values are excluded per test with \code{n_used}
recorded; constant statement vectors are marked "no variation, not
testable". BH false discovery rate adjustment is applied within each family
separately (vs neutral is one family, each attribute is one family).

The gate per attribute is a whole-sort PERMANOVA on Euclidean distances
between sort vectors (pseudo-F, \code{n_perm} label permutations,
R2 = SSB/SST); numeric attributes are median-split solely for the gate.
When a gate fails (p > 0.05) the attribute's table cells read "No overall
group difference" and per-statement flags are suppressed, though adjusted
q values are kept in \verb{$tests}.
}
\examples{
\dontrun{
pr <- compute_priorities(qdata, groups = attribute_groups(qdata))
head(pr$table)
pr$gates
}
}
