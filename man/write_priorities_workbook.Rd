% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorities.R
\name{write_priorities_workbook}
\alias{write_priorities_workbook}
\title{Write the Priorities Workbook}
\usage{
write_priorities_workbook(
  qdata,
  file,
  priorities = NULL,
  groups = NULL,
  top_n = 5
)
}
\arguments{
\item{qdata}{A \code{QsortData} object.}

\item{file}{Output .xlsx path.}

\item{priorities}{Optional precomputed \code{\link[=compute_priorities]{compute_priorities()}} result; when
NULL it is computed from \code{qdata} and \code{groups}.}

\item{groups}{NULL or a named list in the shape \code{attribute_groups()}
returns (see \code{\link[=compute_priorities]{compute_priorities()}}).}

\item{top_n}{Statements per level on the "Top by group" and "Flow" sheets
(default 5).}
}
\value{
Invisibly TRUE on success.
}
\description{
Exports the priorities layer to an Excel workbook with sheets "Ranked"
(the core ranked table), "Group comparisons" (the ranked table with one
label column per tested attribute plus a legend explaining q), "Top by
group" (the \code{\link[=priorities_top_n]{priorities_top_n()}} long table), "Attributes" (the
participant attribute table plus any match report sentences stored in
\code{metadata$attribute_match}), and, when ggalluvial is available and group
attributes exist, "Flow" with the flow chart inserted as a PNG.
}
