% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{bin_attribute}
\alias{bin_attribute}
\title{Bin Attribute Values by a Specification}
\usage{
bin_attribute(values, spec)
}
\arguments{
\item{values}{Vector of raw attribute values}

\item{spec}{A specification list with elements \code{type}, and optionally
\code{levels}, \code{order}, \code{breaks}, \code{labels}, and \code{map}}
}
\value{
A factor of the same length as \code{values}
}
\description{
Apply an attribute specification (as stored in
\code{metadata$attribute_spec}) to a vector of raw values. A numeric spec with
\code{breaks} cuts the values (\code{include.lowest = TRUE}). A spec with a \code{map}
recodes levels and builds a factor in the given \code{order} (ordered when the
type is ordinal or an explicit order is given). Without breaks or map,
the raw values become a factor, ordered by parsed lower bound for
ordinal attributes.
}
\examples{
bin_attribute(c(3, 60, 85), list(type = "numeric", breaks = c(0, 50, 100)))
}
