% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{set_participant_attributes}
\alias{set_participant_attributes}
\title{Set Participant Attributes}
\usage{
set_participant_attributes(qdata, attrs, spec = NULL)
}
\arguments{
\item{qdata}{A QsortData object}

\item{attrs}{A data.frame whose first column is \code{participant}}

\item{spec}{Optional named list of attribute specifications to use instead
of automatic detection; names must be attribute columns of \code{attrs}}
}
\value{
The modified QsortData object
}
\description{
Store a participant attribute table on a \link{QsortData} object. The table
must have \code{participant} as its first column and exactly one row per
participant in \code{qdata@participants}. Rows given in a different order are
realigned; missing or extra participants are an error (use
\code{\link[=attach_participant_attributes]{attach_participant_attributes()}} to match an external file by id).

A type specification is initialized under \code{metadata$attribute_spec} via
\code{\link[=detect_attribute_types]{detect_attribute_types()}} for every attribute column that does not
already have one.
}
\examples{
\dontrun{
attrs <- data.frame(participant = qdata@participants,
                    age = c("18-30", "31-40", "41-50"))
qdata <- set_participant_attributes(qdata, attrs)
}
}
