% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorities.R
\name{priorities_top_n}
\alias{priorities_top_n}
\title{Top or Bottom Statements Per Group Level}
\usage{
priorities_top_n(qdata, groups, n = 5, direction = c("top", "bottom"))
}
\arguments{
\item{qdata}{A \code{QsortData} object.}

\item{groups}{A named list in the shape \code{attribute_groups()} returns, or
NULL to use \code{attribute_groups(qdata)} when attributes exist.}

\item{n}{Number of statements per level (default 5).}

\item{direction}{"top" for the highest means, "bottom" for the lowest.}
}
\value{
A data.frame with columns \code{attribute}, \code{level}, \code{rank}, \code{stmt},
\code{statement}, \code{mean} (subgroup mean), and \code{tied}.
}
\description{
For every level of every factor attribute in \code{groups}, the \code{n} statements
with the highest (or lowest) subgroup mean. Ties inside the block keep
stable statement order, and \code{tied} is TRUE for every rank in a run of
equal means, including the block boundary (rank \code{n} tying rank \code{n + 1}).
Numeric attributes have no levels and are skipped. Levels are descriptive
here, so small levels are included; interpretability is governed by the
PERMANOVA gates in \code{\link[=compute_priorities]{compute_priorities()}}.
}
