% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-core.R
\name{qsort_analyze}
\alias{qsort_analyze}
\title{Complete Q-Sort Analysis}
\usage{
qsort_analyze(
  data,
  nfactors = NULL,
  extraction = c("pca", "centroid", "minres"),
  rotation = c("varimax", "quartimax", "oblimin", "promax", "none"),
  cor_method = c("pearson", "spearman", "kendall"),
  flagging = c("auto", "manual", "theoretical"),
  flag_threshold = NULL,
  forced = TRUE,
  av_rel_coef = 0.8,
  distinguish_criterion = c("all", "any"),
  flag_p_level = 0.05,
  flag_majority = TRUE
)
}
\arguments{
\item{data}{A QsortData object or matrix of Q-sorts}

\item{nfactors}{Number of factors to extract (default: auto-determined)}

\item{extraction}{Extraction method: "pca" (default), "centroid", or "minres"}

\item{rotation}{Rotation method: "varimax" (default), "quartimax", "oblimin",
"promax", or "none"}

\item{cor_method}{Correlation method: "pearson" (default), "spearman", or "kendall"}

\item{flagging}{Flagging method: "auto" (default), "manual", or "theoretical"}

\item{flag_threshold}{For theoretical flagging, the significance threshold}

\item{forced}{Logical; is the distribution forced? (default TRUE)}

\item{av_rel_coef}{Average reliability coefficient for individual Q-sorts
(default 0.8). Used in computing composite reliability via the Spearman-Brown
formula. Standard Q-methodology practice uses 0.8 (Brown, 1980).}
}
\value{
A QsortResults object containing all analysis results
}
\description{
Perform a complete Q-sort analysis including correlation, factor extraction,
rotation, flagging, and computation of factor scores. This is the main
analysis function that wraps all individual steps.
}
\examples{
\dontrun{
# Basic analysis
results <- qsort_analyze(qdata)

# Custom settings
results <- qsort_analyze(
  qdata,
  nfactors = 3,
  extraction = "centroid",
  rotation = "varimax",
  flagging = "auto"
)

# View results
summary(results)
plot(results)
}
}
