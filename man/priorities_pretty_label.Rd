% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorities.R
\name{priorities_pretty_label}
\alias{priorities_pretty_label}
\title{Alluvial Flow Chart of Top Statements Across Group Levels}
\usage{
priorities_pretty_label(x)
}
\arguments{
\item{top_tbl}{A data.frame from \code{\link[=priorities_top_n]{priorities_top_n()}} (columns \code{attribute},
\code{level}, \code{rank}, \code{stmt}, and ideally \code{statement}).}

\item{statements}{Optional statement text lookup: a named character vector
(names are statement codes) or a data.frame whose first two columns are
code and text. Defaults to the \code{statement} column of \code{top_tbl}.}

\item{overall_means}{Optional named numeric vector of overall statement
means (names are statement codes) used to order the palette.}

\item{gates}{Optional \verb{$gates} data.frame from \code{\link[=compute_priorities]{compute_priorities()}}.
A caption marks panels of attributes whose gate failed as descriptive
only; blocks keep their full color either way.}

\item{label_width}{Wrap width for legend labels (default 45).}

\item{palette}{\code{"brand"} (the default: theme factor colors extended with
the prototype's hexes) or \code{"ramp"}: the Q-sort rank ramp, where the
statement with the highest overall mean takes the warm end and the
lowest the navy end, matching the app's pyramid color language.}

\item{show_legend}{Draw the statement-text legend (default TRUE). The
dashboard passes FALSE and reveals statements on click instead.}
}
\value{
A ggplot object. For interactive use, the attributes
\code{priorities_levels} (x-axis column keys in order), \code{priorities_colors}
(statement fill colors), and \code{priorities_nmax} (ranks per column) are
attached so a click position maps back to a statement.
}
\description{
Renders a \code{\link[=priorities_top_n]{priorities_top_n()}} table as an alluvial flow chart in the Kake
prototype's visual grammar: one column per group level (order of
appearance), strata stacked from Rank 1 downward, cubic flows with
front-back lode guidance, white-bordered strata carrying bold statement
codes, and a statement-text legend.

The palette covers only the statements present in \code{top_tbl}, ordered by
\code{overall_means} (descending) when supplied, otherwise by first appearance.
Colors come from the theme factor colors extended with the prototype's
extra hexes and, beyond that, a ramp over the first six brand colors.
}
