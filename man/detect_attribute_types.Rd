% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{detect_attribute_types}
\alias{detect_attribute_types}
\title{Detect Attribute Types}
\usage{
detect_attribute_types(attrs)
}
\arguments{
\item{attrs}{A data.frame of attributes (a \code{participant} column is skipped)}
}
\value{
A named list with one entry per attribute column, each a list
with elements \code{type}, \code{levels}, and \code{order}
}
\description{
Classify every non-participant column of an attribute table as numeric,
ordinal, or categorical. A column is numeric when it is numeric or all
its non-missing values are plain numbers. It is ordinal when all values
are range strings such as \code{"18-30"}, \code{"31 to 40"}, or \code{"71+"}, with
levels ordered by the parsed lower bound. Everything else is categorical
with alphabetically sorted levels.
}
\examples{
detect_attribute_types(data.frame(age = c("18-30", "71+", "31-40")))
}
